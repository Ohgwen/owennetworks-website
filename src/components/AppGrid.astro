---
const apps = [
  { name: 'Authentication Provider', url: 'https://auth.owennetworks.live', icon: '☁️' },
  { name: 'Media Server', url: 'https://fin.owennetworks.live', icon: '🎞️' },
  { name: 'Home Assistant', url: 'https://home.owennetworks.live', icon: '🏠' },
];
---

<section id="apps" class="py-24 bg-[#1A2421] px-6">
  <div class="max-w-6xl mx-auto">
    <h2 class="text-3xl text-white font-semibold mb-12 flex items-center gap-4">
      <span class="h-px w-12 bg-[#C2996B]"></span> 
      Network Directory
    </h2>
    
    <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
      {apps.map(app => (
        <a href={app.url} class="group p-6 bg-white/5 border border-white/10 rounded-xl hover:border-[#C2996B] transition-colors">
          <span class="text-3xl mb-4 block">{app.icon}</span>
          <h3 class="text-xl text-white font-medium">{app.name}</h3>
          <p class="text-gray-400 text-sm mt-2">Access {app.name} instance →</p>
        </a>
      ))}
    </div>
  </div>
</section>