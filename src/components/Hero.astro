---
import { Image } from 'astro:assets';
import heroImage from '../assets/hero-bg.jpg';
import "../styles/global.css";
---

<section class="relative h-screen w-full overflow-hidden flex items-center justify-center">
  <div class="absolute inset-0 -z-10">
    <Image 
      src={heroImage} 
      alt="Ramapo Valley Hike" 
      class="h-full w-full object-cover"
      quality="high"
    />
    <div class="absolute inset-0 bg-black/30"></div>
  </div>

  <div class="absolute inset-10 border-white/20 border-px pointer-events-none hidden md:block">
    <div class="absolute top-0 left-0 w-8 h-8 border-t-2 border-l-2 border-white"></div>
    <div class="absolute top-0 right-0 w-8 h-8 border-t-2 border-r-2 border-white"></div>
    <div class="absolute bottom-0 left-0 w-8 h-8 border-b-2 border-l-2 border-white"></div>
    <div class="absolute bottom-0 right-0 w-8 h-8 border-b-2 border-r-2 border-white"></div>
  </div>

  <div class="z-10 text-center px-4">
    <div class="backdrop-blur-md bg-white/10 p-8 rounded-2xl border border-white/20 shadow-2xl">
      <h1 class="text-5xl md:text-7xl font-bold text-white tracking-tighter mb-4">
        OWEN <span class="text-[#C2996B]">NETWORKS</span>
      </h1>
      <p class="text-lg md:text-xl text-[#E8EEF2] mb-8 font-light">
        Capturing the Wilderness // Architecting the Lab
      </p>
      
      <div class="flex flex-col md:flex-row gap-4 justify-center">
        <a href="#apps" class="px-8 py-3 bg-[#5E6B73] text-white rounded-full hover:bg-[#C2996B] transition-all">
          Launch Apps
        </a>
        <a href="#gallery" class="px-8 py-3 border border-white text-white rounded-full hover:bg-white/10 transition-all">
          View Gallery
        </a>
      </div>
    </div>
  </div>

  <div class="absolute bottom-12 left-12 text-[#E8EEF2]/60 text-xs font-mono hidden lg:block">
    <p>LOC: 41.1340° N, 74.1629° W</p>
    <p>ISO 100 | 35mm | f/2.8</p>
  </div>
</section>